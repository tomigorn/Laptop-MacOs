hosts:
  ".*":
    +s: xxh-shell-fish
    ++pexpect-timeout: "30"
    ++copy-method: scp
    +if:
    +hhh: "~"
    +hhr:
    -o:
      - ControlMaster=auto
      - ControlPath=~/.ssh/cm/xxh-%n
      - ServerAliveInterval=15
      - ServerAliveCountMax=3
